syntax = "proto3";

package test;

message Outer {
  string id = 1;

  message Inner {
    string value = 2;
    string extra_field = 3;  // new field added to nested message
  }

  Inner inner = 3;
}

